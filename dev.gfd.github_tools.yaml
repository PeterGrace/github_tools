app-id: dev.gfd.github_tools
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: github-tools

finish-args:
  - --share=network
  - --device=all

modules:
  - name: github_tools
    buildsystem: simple
    build-options:
      append-path: /usr/lib/sdk/rust-stable/bin
      env:
        CARGO_HOME: /run/build/github_tools/cargo
        RUSTFLAGS: "-C target-feature=-crt-static"
    build-commands:
      - cargo --offline fetch --manifest-path Cargo.toml --verbose
      - cargo --offline build --release --manifest-path Cargo.toml
      - install -Dm755 target/release/github-tools /app/bin/github-tools
    sources:
      - type: dir
        path: .
      - cargo-sources.json
